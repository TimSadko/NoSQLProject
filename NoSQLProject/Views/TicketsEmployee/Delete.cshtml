@model NoSQLProject.Models.Ticket

@{
    ViewData["Title"] = "Delete Ticket";
}

<partial name="_ErrorMessage"/>

<form action="/TicketsEmployee/Delete" method="post">
    <input type="hidden" asp-for="Id"/>
    <div class="form-group py-2 text-start">
        <label for="Title">Title:</label>
        <input type="text"
               id="Title"
               name="Title"
               class="form-control input-background"
               value="@Model.Title"
               required
               readonly/>
    </div>

    <div class="form-group py-2 text-start">
        <label for="Description">Description:</label>
        <input type="text" id="Description"
               name="Description"
               class="form-control input-background"
               value="@Model.Description"
               required
               readonly/>
    </div>

    <button type="submit" class="primary-button">Delete</button>
</form>

<a href="/TicketsServiceDesk/Index">Back to Ticket List</a>
