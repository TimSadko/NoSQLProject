@if (TempData["Success"] != null)
{
    <div class="alert alert-success fade-in-alert">
        @TempData["Success"]
    </div>
}

@if (ViewData["Exception"] != null)
{
    <div class="alert alert-danger">
        @ViewData["Exception"]
    </div>
}

@if (TempData["Exception"] != null)
{
    <div class="alert alert-danger">@TempData["Exception"]</div>
}
