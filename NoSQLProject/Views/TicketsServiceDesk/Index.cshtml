@model NoSQLProject.Models.SDETickestsListViewModel

@{
	ViewData["Title"] = "Tickets";
}

<partial name="_ErrorMessage" />

<h1>Tickets:</h1>
<a href="/TicketsServiceDesk/Add">Add new Ticket</a>

<table class="table">
	<thead>
		<tr>
			<th>Title</th>
			<th>Status</th>
			<th>Created At</th>
			<th>Last Update</th>
			<th>Created By</th>
			<th>logs</th>
			<th>Actions</th>
		</tr>
	</thead>
	<tbody>
		@{
			for (int i = 0; i < Model.Tickets.Count; i++)
			{
				<tr>
					<th>@Model.Tickets[i].Title</th>
					<th>@Model.Tickets[i].Status</th>
					<th>@Model.Tickets[i].CreatedAt</th>
					<th>@Model.Tickets[i].UpdatedAt</th>
					<th>@(Model.Employees[i] == null ? "???" : Model.Employees[i].FullName)</th>
					<th>@Model.Tickets[i].Logs.Count</th>
					<th>
						<a href="/TicketsServiceDesk/Edit/@Model.Tickets[i].Id">View</a>
						<a class="text-danger" href="/TicketsServiceDesk/Delete/@Model.Tickets[i].Id">Delete</a>
					</th>
				</tr>
			}
		}
	</tbody>
</table> *@


@model List<NoSQLProject.Models.Ticket>

@{
    ViewData["Title"] = "Tickets (Service Desk)";
}

<h1>Tickets (Service Desk)</h1>
<a href="/TicketsServiceDesk/Add" class="btn btn-primary mb-2">Add new Ticket</a>

<!-- ✅✅✅ Added by TAREK — Sorting links for all ticket fields -->
<!-- Allows Service Desk employees to sort tickets by Title, Status, CreatedAt, UpdatedAt -->
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                <a href="?sortField=Title&sortOrder=1">Title ↑</a> |
                <a href="?sortField=Title&sortOrder=-1">↓</a>
            </th>
            <th>
                <a href="?sortField=Status&sortOrder=1">Status ↑</a> |
                <a href="?sortField=Status&sortOrder=-1">↓</a>
            </th>
            <th>
                <a href="?sortField=CreatedAt&sortOrder=-1">Created ↓</a>
            </th>
            <th>
                <a href="?sortField=UpdatedAt&sortOrder=-1">Updated ↓</a>
            </th>
            <th>Created By</th>
            <th>Logs</th>
            <th>Actions</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var t in Model)
        {
            <tr>
                <td>@t.Title</td>
                <td>@t.Status</td>
                <td>@t.CreatedAt.ToString("g")</td>
                <td>@t.UpdatedAt.ToString("g")</td>
                <td>@t.CreatedById</td>
                <td>@t.Logs?.Count</td>
                <td><a href="/TicketsServiceDesk/Edit/@t.Id" class="btn btn-sm btn-outline-info">View</a></td>
            </tr>
        }
    </tbody>
</table>
<!-- ✅ END of TAREK’s sorting implementation -->
